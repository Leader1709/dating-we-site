# Dating Web Application

## Setup Instructions

1. Install dependencies:
   ```bash
   npm install
   ```

2. Set up MySQL database using `schema.sql`.

3. Update `.env` with your database and email credentials.

4. Start the server:
   ```bash
   npm start
   ```

5. Open `http://localhost:3000` in your browser.
